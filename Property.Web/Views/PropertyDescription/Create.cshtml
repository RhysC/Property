@model Property.Web.Models.Landlord.PropertyDescription

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Property Description</legend>
        
        @Html.Partial("_AddressCreate", new ViewDataDictionary { TemplateInfo = new TemplateInfo { HtmlFieldPrefix = "Address" } })
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to Portfolio", "Index", "Portfolio")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
